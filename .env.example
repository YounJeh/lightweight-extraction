# Choisir le modèle utilisé (vide = défaut LangExtract, actuellement
# gemini-3.5-flash — voir app/tools/ner_langextract.py). Format attendu :
# un model_id LangExtract/Gemini brut (ex. "gemini-3.5-flash"), pas un
# identifiant préfixé par un fournisseur (ex. "google/...", style OpenRouter).
LLM_MODEL=

# Clé google
GOOGLE_GENERATIVE_AI_API_KEY=